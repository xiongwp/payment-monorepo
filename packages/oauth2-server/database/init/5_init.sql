-- AUTO-GENERATED by generate.sh — do not edit by hand
-- 物理库: oauth2_db_5
-- 持有: clients_50..59, revoked_tokens_50..59, admin_audit_50..59

SET NAMES utf8mb4;
CREATE DATABASE IF NOT EXISTS `oauth2_db_5` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
USE `oauth2_db_5`;

-- ─── clients_50 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_50` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 50)';

-- ─── revoked_tokens_50 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_50` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 50)';

-- ─── admin_audit_50 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_50` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 50)';

-- ─── clients_51 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_51` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 51)';

-- ─── revoked_tokens_51 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_51` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 51)';

-- ─── admin_audit_51 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_51` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 51)';

-- ─── clients_52 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_52` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 52)';

-- ─── revoked_tokens_52 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_52` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 52)';

-- ─── admin_audit_52 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_52` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 52)';

-- ─── clients_53 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_53` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 53)';

-- ─── revoked_tokens_53 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_53` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 53)';

-- ─── admin_audit_53 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_53` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 53)';

-- ─── clients_54 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_54` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 54)';

-- ─── revoked_tokens_54 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_54` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 54)';

-- ─── admin_audit_54 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_54` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 54)';

-- ─── clients_55 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_55` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 55)';

-- ─── revoked_tokens_55 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_55` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 55)';

-- ─── admin_audit_55 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_55` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 55)';

-- ─── clients_56 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_56` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 56)';

-- ─── revoked_tokens_56 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_56` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 56)';

-- ─── admin_audit_56 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_56` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 56)';

-- ─── clients_57 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_57` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 57)';

-- ─── revoked_tokens_57 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_57` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 57)';

-- ─── admin_audit_57 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_57` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 57)';

-- ─── clients_58 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_58` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 58)';

-- ─── revoked_tokens_58 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_58` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 58)';

-- ─── admin_audit_58 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_58` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 58)';

-- ─── clients_59 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_59` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 59)';

-- ─── revoked_tokens_59 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_59` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 59)';

-- ─── admin_audit_59 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_59` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 59)';

-- ────────────────── shard 5 init done ──────────────────
