-- AUTO-GENERATED by generate.sh — do not edit by hand
-- 物理库: oauth2_db_3
-- 持有: clients_30..39, revoked_tokens_30..39, admin_audit_30..39

SET NAMES utf8mb4;
CREATE DATABASE IF NOT EXISTS `oauth2_db_3` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
USE `oauth2_db_3`;

-- ─── clients_30 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_30` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 30)';

-- ─── revoked_tokens_30 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_30` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 30)';

-- ─── admin_audit_30 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_30` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 30)';

-- ─── clients_31 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_31` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 31)';

-- ─── revoked_tokens_31 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_31` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 31)';

-- ─── admin_audit_31 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_31` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 31)';

-- ─── clients_32 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_32` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 32)';

-- ─── revoked_tokens_32 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_32` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 32)';

-- ─── admin_audit_32 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_32` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 32)';

-- ─── clients_33 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_33` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 33)';

-- ─── revoked_tokens_33 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_33` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 33)';

-- ─── admin_audit_33 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_33` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 33)';

-- ─── clients_34 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_34` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 34)';

-- ─── revoked_tokens_34 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_34` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 34)';

-- ─── admin_audit_34 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_34` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 34)';

-- ─── clients_35 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_35` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 35)';

-- ─── revoked_tokens_35 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_35` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 35)';

-- ─── admin_audit_35 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_35` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 35)';

-- ─── clients_36 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_36` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 36)';

-- ─── revoked_tokens_36 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_36` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 36)';

-- ─── admin_audit_36 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_36` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 36)';

-- ─── clients_37 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_37` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 37)';

-- ─── revoked_tokens_37 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_37` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 37)';

-- ─── admin_audit_37 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_37` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 37)';

-- ─── clients_38 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_38` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 38)';

-- ─── revoked_tokens_38 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_38` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 38)';

-- ─── admin_audit_38 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_38` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 38)';

-- ─── clients_39 ────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `clients_39` (
  `id`              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `client_id`       VARCHAR(128)    NOT NULL,
  `secret_hash`     VARCHAR(255)    NOT NULL,
  `secret_last4`    VARCHAR(8)      NOT NULL,
  `name`            VARCHAR(255)    NOT NULL,
  `owner_type`      ENUM('merchant','service','ops') NOT NULL,
  `owner_id`        VARCHAR(128)    NOT NULL,
  `allowed_scopes`  TEXT            NOT NULL,
  `allowed_ips`     TEXT            NULL,
  `rate_limit_rps`  INT             NOT NULL DEFAULT 0,
  `status`          ENUM('active','suspended','revoked') NOT NULL DEFAULT 'active',
  `expires_at`      DATETIME        NULL,
  `last_used_at`    DATETIME        NULL,
  `created_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at`      DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_owner`         (`owner_type`, `owner_id`),
  KEY `idx_status`        (`status`),
  KEY `idx_last_used`     (`last_used_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='OAuth2 客户端 (shard 39)';

-- ─── revoked_tokens_39 ─────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `revoked_tokens_39` (
  `jti`         VARCHAR(128) NOT NULL,
  `revoked_at`  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `expires_at`  DATETIME     NOT NULL,
  `client_id`   VARCHAR(128) NULL,
  PRIMARY KEY (`jti`),
  KEY `idx_expires` (`expires_at`),
  KEY `idx_client`  (`client_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='token revocation 黑名单 (shard 39)';

-- ─── admin_audit_39 ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS `admin_audit_39` (
  `id`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `occurred_at`  DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `actor`        VARCHAR(128)    NOT NULL,
  `action`       VARCHAR(64)     NOT NULL,
  `client_id`    VARCHAR(128)    NULL,
  `source_ip`    VARCHAR(64)     NULL,
  `details`      JSON            NULL,
  `prev_hash`    CHAR(64)        DEFAULT NULL COMMENT 'per-actor chain head',
  `row_hash`     CHAR(64)        NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_actor`     (`actor`),
  KEY `idx_client`    (`client_id`),
  KEY `idx_occurred`  (`occurred_at`),
  KEY `idx_action`    (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='admin 操作审计 (shard 39)';

-- ────────────────── shard 3 init done ──────────────────
