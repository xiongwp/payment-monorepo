apiVersion: v1
kind: Service
metadata:
  name: payment-core
  labels: { app: payment-core }
spec:
  type: ClusterIP
  selector: { app: payment-core }
  ports:
    - { name: grpc,    port: 9090, targetPort: grpc }
    - { name: metrics, port: 9190, targetPort: metrics }
---
# headless 副本：grpc-go 客户端的 dns:/// resolver 走它，可拿到所有 endpoints
# 做客户端 LB（avoid Service 单点 5-tuple-hash 偏分布）。
apiVersion: v1
kind: Service
metadata:
  name: payment-core-headless
  labels: { app: payment-core }
spec:
  clusterIP: None
  selector: { app: payment-core }
  ports:
    - { name: grpc, port: 9090, targetPort: grpc }
