apiVersion: v1
kind: Service
metadata:
  name: payment-admin-web
  labels: { app: payment-admin-web }
spec:
  type: ClusterIP
  selector: { app: payment-admin-web }
  ports:
    - { name: grpc,    port: 9190, targetPort: grpc }
    - { name: metrics, port: 9291, targetPort: metrics }
---
# headless 副本：grpc-go 客户端的 dns:/// resolver 走它，可拿到所有 endpoints
# 做客户端 LB（avoid Service 单点 5-tuple-hash 偏分布）。
apiVersion: v1
kind: Service
metadata:
  name: payment-admin-web-headless
  labels: { app: payment-admin-web }
spec:
  clusterIP: None
  selector: { app: payment-admin-web }
  ports:
    - { name: grpc, port: 9190, targetPort: grpc }
