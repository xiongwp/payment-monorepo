apiVersion: v1
kind: Service
metadata:
  name: risk-manage
  labels: { app: risk-manage }
spec:
  type: ClusterIP
  selector: { app: risk-manage }
  ports:
    - { name: grpc,    port: 9490, targetPort: grpc }
    - { name: metrics, port: 9590, targetPort: metrics }
---
# headless 副本：grpc-go 客户端的 dns:/// resolver 走它，可拿到所有 endpoints
# 做客户端 LB（avoid Service 单点 5-tuple-hash 偏分布）。
apiVersion: v1
kind: Service
metadata:
  name: risk-manage-headless
  labels: { app: risk-manage }
spec:
  clusterIP: None
  selector: { app: risk-manage }
  ports:
    - { name: grpc, port: 9490, targetPort: grpc }
