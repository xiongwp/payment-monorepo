# 模板：用 sealed-secrets / external-secrets / kustomize secretGenerator 生成真实 Secret。
# 不要把这个文件的真值直接 git commit。
apiVersion: v1
kind: Secret
metadata:
  name: kms-manage-secret
type: Opaque
stringData:
  USERMERCHANTCORE_DATABASE_META_DSN: "root:CHANGEME@tcp(meta-mysql:3306)/user_merchant_meta?charset=utf8mb4&parseTime=True&loc=Local"
  USERMERCHANTCORE_KMS_BEARER_TOKEN:  "CHANGEME"
